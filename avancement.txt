Datastructures& algorithms

Gradle
Git   wselt l hna

Github
gitlab
bitbucket



Git Tutoriel:
$ git config --global user.name "John Doe"
$ git config --global user.email johndoe@example.com
$ git config --global core.editor <namelog> //faire le lien entre Git et l'editeur de texte
$git init <project directory> //Pointing git init to an existing project directory will execute the same initialization setup as mentioned above, but scoped to that project directory
$git clone <repo url> //git clone is used to create a copy or clone of remote repositories. You pass git clone a repository URL. 

t e s t   c o n t e n t   f o r   g i t   t u t o r i a l 

$echo "test content for git tutorial" >> avancement.txt // pour ecrire test content for git tutorial dans avancement.txt apres avoir utiliser cd pour aller au dossier du fichier
$git status
$git log // to show the history of commits in our projects 
$git add avancement.txt // to save the changes 
$ vi avancement.txt // to write int avancement.txt
$ cat avancement.txt // to show us the content of our file

$git reset SHA // awel haja SHA heya "11b87f1dbb8fd467e3411d2d82a19fd121d13dfa" id taa wahda ml history w ki naaml reset SHA li jew baad l SHA heki yetfaskhou lkol
$ git stash // is about to add the modification not to the history off our repos but to the back of our repos
$ git stash pop // is about to get the modificcation from the back to the front